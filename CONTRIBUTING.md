# Contributing

## Branch workflow

- `main` is the release-ready branch and is updated only through reviewed pull requests.
- `development` is the integration branch for ongoing work.
- Create feature and fix branches from the latest `development` branch.
- Open pull requests back into `development` and keep each pull request focused.
- Merge `development` into `main` through a release pull request.
